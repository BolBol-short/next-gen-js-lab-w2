/* ================================================================
   JavaScript — Week 2 — Lab 1 · Exercise 2 (data types)  [BARE-SPEC]
   ----------------------------------------------------------------
   TASK
     1. Store the typeof each value in these variables:
          typeText  = the type of "hello"
          typeNum   = the type of 42
          typeBool  = the type of true
     2. Then make:
          notSet    = declared but NOT assigned (stays undefined)
          empty     = null
          fromText  = the NUMBER 50, converted from the string "50"
     3. Print all six.

   EXPECTED OUTPUT
     typeText: string
     typeNum: number
     typeBool: boolean
     notSet: undefined
     empty: null
     fromText: 50

   RUN:  node assignment/lab1/exercise2.js
   ================================================================ */

// Write your code below.