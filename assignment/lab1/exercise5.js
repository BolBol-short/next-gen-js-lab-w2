/* ================================================================
   JavaScript — Week 2 — Lab 1 · Exercise 5 (ternary + alert)  [BARE-SPEC]
   ----------------------------------------------------------------
   TASK
     `temp` is 30.
     1. Use a ternary to set `reaction` to "Hot" if temp > 25,
        otherwise "Cool".
     2. Show the reaction with alert(reaction).
     3. Also print reaction to the console.

   EXPECTED OUTPUT
     A pop-up saying:  Hot
     Console:          reaction: Hot

   RUN (alert needs a browser, not Node):
     open index.html with Live Server / Live Preview, then F12 for console
   ================================================================ */

// Write your code below.