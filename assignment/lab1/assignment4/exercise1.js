/* ================================================================
   Assignment 4: prompt  ·  Exercise 1  (capture text)
   ----------------------------------------------------------------
   Ask the user their name with prompt and store it in `userName`.
   (When testing, prompt is answered automatically — no pop-up.)
   Test it by running:   npm test        (or: node protected/lab1/run-tests.js)
   ================================================================ */

let userName = "";   // TODO: userName = prompt("What is your name?")